# TeamBuildingSystem 
GDGoC 4기 멤버들이 진행하는 '팀빌딩 프로젝트'의 백엔드 레포지토리 입니다.

## 로컬 환경에서 테스트하는 방법
1. `git clone`명령어를 통해 레포지토리의 파일들을 가져온다.
2. `.env.example`의 포맷을 참고해 내용을 작성한다.
3. 루트 디렉토리에 `.env` 파일을 생성한다.
4. 2번에서 작성한 내용을 `.env` 파일에 붙여 넣는다.
